# Route inventory

_Generated by `scripts/scan-routes.mjs` on 2026-07-12T13:12:54.274Z_

Total routes: **7** (filtered by `canteen`)

> **Read this before scaffolding any new page.** Look up the concept you're about to build. If a route already exists that owns the concept, extend it — do not duplicate.

## Yard / Trade-off (legacy — becoming Trade Center)

| URL | Purpose (from source comment) | File |
|---|---|---|
| `/trade-off/yard/canteens` | /trade-off/yard/canteens — Canteens index. Inline with the Yard. Public list of canteens, grouped by trade, with a "Start a canteen" CTA for hosts. First-100 badges attach here. | `src/app/trade-off/yard/canteens/page.tsx` |
| `/trade-off/yard/canteens/[slug]` | /trade-off/yard/canteens/[slug] — Canteen detail page. Two-column on desktop: main feed on the left, The Counter on the right. Mobile: header → side-lane horizontal strip → main feed stack. | `src/app/trade-off/yard/canteens/[slug]/page.tsx` |
| `/trade-off/yard/canteens/[slug]/about` | /trade-off/yard/canteens/[slug]/about — read-only "About this canteen". Linked from the CanteenHeader's About chip. Renders canteen banner, name, tagline, host card, member/activity stats, founding-100 badge, and a back- | `src/app/trade-off/yard/canteens/[slug]/about/page.tsx` |
| `/trade-off/yard/canteens/[slug]/manage` | /trade-off/yard/canteens/[slug]/manage — host dashboard. Access: currently mocked to always allow (real gating lands with the auth + host-verification schema). Renders the CanteenManageShell with the current canteen + pr | `src/app/trade-off/yard/canteens/[slug]/manage/page.tsx` |
| `/trade-off/yard/canteens/[slug]/products` | /trade-off/yard/canteens/[slug]/products — full products list. Reached from the canteen page's "View all N products" link and the header's "View products" primary CTA. Renders every product the host has listed in this ca | `src/app/trade-off/yard/canteens/[slug]/products/page.tsx` |
| `/trade-off/yard/canteens/[slug]/products/[productId]` | /trade-off/yard/canteens/[slug]/products/[productId] — canteen PDP. Dedicated product detail page for a canteen product. Reached from the product grid's "View details" CTA on each card. Composition mirrors the Trade Cent | `src/app/trade-off/yard/canteens/[slug]/products/[productId]/page.tsx` |
| `/trade-off/yard/canteens/new` | /trade-off/yard/canteens/new — canteen creation form. The "Start a Canteen" CTA on the index routes here. The form is a thin client shell over POST /api/canteens/create. Anonymous callers see an inline nudge to log in (t | `src/app/trade-off/yard/canteens/new/page.tsx` |

