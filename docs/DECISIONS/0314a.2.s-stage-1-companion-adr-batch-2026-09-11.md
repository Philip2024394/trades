# Stage 1 Companion ADR Batch · 2026-09-11

**Status:** Doctrine authoring · Gate 3 remains CLOSED · no code · no substrate mutation · no migration · no verifier implementation · fixtures NOT populated · R-10 impact analysis presented for founder review
**Founder authorisation (verbatim 2026-09-11):**

> *"AUTHORISE STAGE 1. Founder authorises Stage 1 — Truth Engine Verifier + Guardian Foundation. Gate 3 remains CLOSED while the 12 Stage 1 companion ADRs are authored and presented for founder review. Do not write implementation code, modify production substrates, run migrations, change existing rows, or open Gate 3 merely because the ADRs are authored. First task: Author the 12 Stage 1 companion ADRs. Present each ADR's purpose, dependency, proposed rule, substrate impact, acceptance criteria, and founder decision points. Reconcile them against the frozen constitution and all locked R-01–R-20 decisions. Produce the Stage 1 fixture strategy/reference set. Produce the R-10 impact analysis required by Stage 1. Stop and request explicit founder approval before any substrate write, migration, verifier implementation, or Gate 3 opening. Gate 3 remains CLOSED until the founder explicitly issues GATE 3 OPEN."*

**Founder-provided constitutional safeguard:**

> *"I would not let 'all 12 ADRs founder-authorised' automatically mean Gate 3 opens. The plan already says the correct thing: the founder must separately issue GATE 3 OPEN. That is the actual constitutional write-authorisation event."*

**Parent documents:**
- Constitutional layer: `docs/DECISIONS/0314a.2.s-canonical-domain-and-category-taxonomies.md` (§7.1–§7.10 · frozen)
- Constitutional Completion Audit: `docs/DECISIONS/0314a.2.s-constitutional-completion-audit-2026-09-11.md`
- Full Implementation Plan: `docs/DECISIONS/0314a.2.s-full-implementation-plan-2026-09-11.md` (6 stages · 14 tests)

---

## Part A · Reconciliation against the frozen constitution

Each Stage 1 companion ADR consumes locked constitutional inputs. This reconciliation confirms no companion ADR contradicts or amends any locked rule.

| Locked input | Consumed by |
|---|---|
| R-DOMAIN-01 (3-axis anti-substitution) | Every companion ADR · applies at Guardian boundary |
| §7.3-B1 (5-axis architecture) | Every companion ADR · specialist tier maps to LAM Object Type axis |
| §7.6.5 G4 (Activity ≠ Domain) | R-07 (connections) · R-13 (relationships) · R-20 (contradictions across activity attributions) |
| §7.7 H1 (`other` ≠ `unknown` axis-specific) | R-12 (classification enum) · fixture set (unknown vs other cases) |
| §7.8 J1 (Physical ≠ logical authority · Rule 15) | ADR-0314c (Cross-Substrate Contradiction Detection) reads physical values via Router boundary · never as authoritative |
| §7.9 L1 (Business ≠ Services · sub-services are Categories) | R-01 (per-Domain plausibility for Services) · R-05 (Services-scope authorities) |
| §7.10 M1 (Domain axis closed at 9) | R-01 thresholds authored per 9 Domains · R-05 applicable_domain(s) values constrained to 9 Domains |
| R-10 promotion-gate doctrine | Explicitly NOT consumed by Stage 1 (Stage 2 authors R-10 policies) · but Stage 1 verifier verdicts terminate at `truth_engine_ok` per R-10 doctrine · Guardian rejects promotion to AUTHORITATIVE without Stage 2 policy |
| R-11 (confidence numeric authoritative) | ADR-0314a.2.j (band derivation) |
| R-17 (versioning) | ADR-0314a.2.m (thresholds and significance triggers) |
| R-18 (verifier identity + rule_set_version per verdict) | ADR-0314e (verifier implementation) |
| Six feedback memories | Each companion ADR respects: no linguistic-only decisions · no orphan-inflation · no other-vs-unknown conflation · no Activity-as-Domain · no Accommodation-owns-rental · finish taxonomy before 100K corpus |

**Reconciliation verdict: 🟢 CLEAN.** No conflict between the 12 companion ADRs authored below and the frozen constitutional layer. Each ADR is a per-rule *implementation specification* consuming already-locked doctrine.

---

## Part B · 12 companion ADRs (spec form · founder reviews each)

### ADR-0314a.2.j · Confidence Band Derivation Thresholds (R-11)

**Purpose:** Define the deterministic mapping from numeric `confidence_score` (0–100 · authoritative per R-11.v1.0.0) into named confidence bands used by Chat · NEX1 · retrieval surfaces.

**Dependency:** R-11.v1.0.0 (numeric score is authoritative · band derived at Router boundary per §7.8 J1 Rule 15) · existing image-band precedent from ADR-0035 Two Laws (7-band Classify Never Reject model at image-knowledge scope).

**Proposed rule (draft · founder amends):**
- Bands: `very_high` (99+) · `high` (95–98) · `good` (85–94) · `moderate` (70–84) · `low` (50–69) · `very_low` (<50) · `unknown` (score not authored · fail-closed)
- Guardian rejects rows where numeric score and band are inconsistent (band drift check)
- Router applies band-derivation at boundary; Chat/NEX1 receive both numeric + band

**Substrate impact:** writes to `nex.confidence_scores` (schema-ready · currently 0 rows) · Guardian rule installation at Router boundary · no CHECK constraint added by this ADR alone (constraint installed at Stage 1 code time).

**Acceptance criteria:**
- Fixture rows with authored scores map deterministically to expected bands
- Guardian rejects a write where band != derived(score)
- Test N (fail-closed): score=null → band=`unknown` · Guardian rejects promotion of `unknown`-banded row to AUTHORITATIVE

**Founder decision points:**
- Exact numeric thresholds (draft above · founder authors final)
- Whether `unknown` is a valid band value or fail-closed reject
- Language-specific band labels (EN vs ID)
- Alignment with or intentional divergence from ADR-0035 Two Laws image-band model

---

### ADR-0314a.2.k · Classification Taxonomy Enum Values (R-12)

**Purpose:** Author the canonical per-Domain classification taxonomy that governs Category values across the 9 Domains.

**Dependency:** R-12.v1.0.0 · §7.4 C1 (Shared cross-Domain Activity Category · applicable_domains pattern) · §7.7 H1 (axis-specific `other` sentinel) · §7.9 L1 (Services sub-taxonomy: Dentist · Hairdresser · Salon · Car Repair · Gym · Optician · Massage · Pharmacy · etc.) · §7.10 M1 (Domain axis closed at 9).

**Proposed rule (draft):**
- Per-Domain Category enum authored: accommodation Categories (hotel · villa · guesthouse · homestay · resort · hostel · apartment · kos + existing category-registry entries) · food · transport · business · travel · attractions · code · commerce · services
- Cross-Domain Activity Categories authored per §7.4 C1 with per-value `applicable_domains: string[]` (deferred population per ADR-0314a.2.v shell)
- Guardian rejects unregistered Category values (fail-closed to `unknown_category` per §7.7 H1)
- `other` conditionally permitted at Category axis only when parent taxonomy authors it explicitly as a bounded catch-all (per §7.7 H1)

**Substrate impact:** authoring only in this ADR · population and CHECK constraint installation in Stage 3 (per Full Implementation Plan). Guardian rule at classification write in Stage 1 requires the enum values to be REGISTERED before Guardian rejects unregistered · initial registration happens in ADR-0314a.2.k population.

**Acceptance criteria:**
- Unregistered classification values rejected at Guardian
- Registered values pass
- Services sub-Categories (Dentist · Hairdresser · Salon · etc.) authorable per §7.9 L1
- Anti-dumping-ground rule wired for `other` at Category axis (Stage 3 authors threshold rule per ADR-0314a.2.z)

**Founder decision points:**
- Exact enum values per Domain (initial population set)
- Whether `other` is authored per Domain OR globally-shared with per-Domain opt-in
- Alignment with existing category-registry values (per accommodation)
- Naming conventions (English canonical · Indonesian localised labels · UK expansion terms)

---

### ADR-0314a.2.l · Relationship Vocabulary Enum Values (R-13)

**Purpose:** Author the canonical relationship vocabulary governing `relation_kind` values across NEX substrates.

**Dependency:** R-13.v1.0.0 · Bridge relations per ADR-0312 · §7.6.5 G4 (Activity-as-relation cross-Domain) · existing `nex.relationships.relation_kind` CHECK enum with 8 values (synonym · antonym · hypernym · hyponym · related · domain_of · derived_from · part_of).

**Proposed rule (draft):**
- Ratify existing 8-value CHECK constraint as canonical baseline
- Optional extensions per Bridge (ADR-0312): `bridges_to` · `physical_representation_of` · `cross_substrate_alias` (naming subject to founder decision)
- Optional extensions per §7.6.5 G4 activity-relations: `rents_offered_by` · `service_provided_by` · `entity_at_location` (naming subject to founder decision)
- Domain-scoped extensions per §7.4 C1 pattern (`applicable_domains: string[]` per relation kind)
- Guardian rejects unregistered `relation_kind` values (fail-closed per §7.7 H1 at relation axis)

**Substrate impact:** potential CHECK constraint expansion at `nex.relationships.relation_kind` · Guardian rule installation · authoring only in this ADR (constraint modification at Stage 1 code time).

**Acceptance criteria:**
- Unregistered `relation_kind` values rejected
- Existing 8 values continue to pass
- Bridge-mediated relations expressible per ADR-0312
- Cross-Domain relations traceable

**Founder decision points:**
- Exact extension vocabulary beyond the 8 baseline values
- Bridge relation naming
- Activity-relation naming per §7.6.5 G4
- Whether relation-kind CHECK constraint is modified or replaced with a governed lookup table

---

### ADR-0314a.2.m · Versioning Thresholds and Significance Triggers (R-17)

**Purpose:** Define the deterministic threshold rules for what constitutes a *significant change* to a canonical row · triggering an entry in `nex.record_versions`.

**Dependency:** R-17.v1.0.0 · R-18.v1.0.0 (rule_set_version composition · every version records verifier_instance_id) · §7.7 H1 (`other`/`unknown` are distinct · version transitions between them must be explicit).

**Proposed rule (draft):**
- Automatic versioning triggers on:
  - Body content change > 30% (Levenshtein or diff-based metric · founder decides algorithm)
  - Any claim delta at attribute-tier (e.g. `star_rating: 4 → 5`)
  - Authority change (`source_reference` change)
  - Status transition (DRAFT → UNDER_REVIEW → AUTHORITATIVE → DEPRECATED)
  - Domain reclassification (rare · Guardian-authored only)
  - Founder-declared significance (always creates a version regardless of automatic thresholds)
- Version rows carry: `version_number` · `previous_version_id` · `rule_set_version` (per R-18) · `verifier_instance_id` · `change_summary` · `changed_by` · `changed_at`
- Version content is immutable snapshot per LAM Rule 5 provenance
- Fail-closed default: automatic versioning DISABLED until thresholds authored (per R-17 fail-closed clause)

**Substrate impact:** writes to `nex.record_versions` (schema-ready · currently 0 rows) · Guardian rule at write-time to trigger version creation on significant change.

**Acceptance criteria:**
- Significant changes trigger version rows
- Non-significant changes do NOT trigger versions
- Founder-declared changes always create version regardless of threshold
- Version rows carry complete R-18 verifier provenance

**Founder decision points:**
- Exact numeric thresholds (30% body diff · claim-delta significance level)
- Diff algorithm (Levenshtein · semantic-similarity · Jaccard · founder chooses)
- Which status transitions warrant versioning (all · promotion-only · demotion-only)
- Retention policy for version rows (all-time · N years · founder decides)

---

### ADR-0314a.2.n · Per-Domain Plausibility Thresholds (R-01)

**Purpose:** Author per-Domain numeric-claim plausibility threshold tables. Guardian rejects claims that violate thresholds.

**Dependency:** R-01.v1.0.0 · 9-Domain axis locked (§7.10 M1) · §7.9 L1 (Services · specialised thresholds per sub-Category) · country-scoped extensions per §7.10 M1 Location metadata.

**Proposed rule (draft · per Domain · initial values indicative):**

| Domain | Attribute | Plausibility threshold | Fail-closed |
|---|---|---|---|
| accommodation | star_rating | 0–5 | reject <0 or >5 |
| accommodation | rooms | 1–5,000 (upper bound generous for large resorts) | reject <1 or >5,000 |
| accommodation | price_per_night_idr | 0–100,000,000 (100M IDR ~= £5,000 · generous UK-luxury) | reject <0 |
| food | tables | 1–2,000 | reject <1 or >2,000 |
| food | price_per_meal_idr | 0–10,000,000 | reject <0 |
| commerce | price_idr (mp_product) | 0–100,000,000,000 (industrial goods) | reject <0 |
| commerce | stock | 0–1,000,000 | reject <0 |
| services | dentist chair count | 1–100 | reject <1 |
| services | pharmacy sq_m | 1–10,000 | reject <1 |
| transport | seats (vehicle) | 1–500 (bus max) | reject <1 or >500 |
| transport | distance_km (route) | 0.1–20,000 (world) | reject <0.1 |
| business | employee_count | 0–1,000,000 | reject <0 |
| travel | trip_duration_days | 1–365 | reject <1 |
| attractions | admission_fee_idr | 0–10,000,000 | reject <0 |
| code | (no numeric-claim plausibility currently applicable · code-Domain concepts don't have numeric quantities in the R-01 sense) | — | — |

- Fail-closed default: `plausibility_check_disabled_pending_thresholds` when Domain × attribute lacks authored threshold
- Country-scoped variants per Location cross-Domain metadata (UK vs ID · UK price_per_night_gbp separate threshold table)

**Substrate impact:** authoring only in this ADR · Guardian threshold-check rule installation at Stage 1 code time · rejection reason recorded per R-18.

**Acceptance criteria:**
- Implausible values rejected (e.g. `star_rating = 47` · `price_per_night_idr = -100`)
- Fail-closed when threshold unauthored
- Country-scoped thresholds when different (UK vs ID pricing)

**Founder decision points:**
- Exact threshold values per Domain × attribute
- UK/USA/Australia/Ireland threshold tables (Stage 6 authoring · but structure authored here)
- Currency-conversion policy (IDR vs GBP vs USD · founder decides)
- Which sub-Category thresholds within Services (dentist chair count · gym floor area · etc.)

---

### ADR-0314a.2.p · External Authority Registry (R-05)

**Purpose:** Author the canonical registry of permitted external authorities (Approved Docs · BS · EN · WHO · UK Building Regulations · Indonesia kemenparekraf · etc.).

**Dependency:** R-05.v1.0.0 · 9-Domain axis · §7.10 M1 Location cross-Domain metadata (authorities are typically country-scoped).

**Proposed rule (draft · initial seed values indicative):**

| authority_slug | applicable_domain(s) | licence | reliability_score | geography_scope | attribution_required |
|---|---|---|---|---|---|
| `bs_en_1090` | code / accommodation / services (construction) | commercial | 90 | GB · EU | ✅ |
| `who_healthcare` | services (healthcare-adjacent · pharmacies · dentists · clinics) | public | 95 | global | ✅ |
| `kemenparekraf` | accommodation / attractions / travel (Indonesia tourism ministry) | gov | 90 | ID | ✅ |
| `uk_building_regulations` | code / accommodation / services (construction) | gov | 95 | GB | ✅ |
| `bmkg` | transport (Indonesian meteorological) | gov | 90 | ID | ✅ |
| `osm_openstreetmap` | accommodation / food / transport / attractions (existing Lab source) | ODbL | 75 | global | ✅ |
| `wikidata` | attractions / travel / cultural (existing Lab source) | CC0 | 80 | global | ✅ |
| `msme_registry` | business / services (existing Lab source) | gov | 85 | ID | ✅ |

- Fail-closed default: `authority_check_disabled_pending_registry` · `unregistered_authority` result for cited-but-unregistered authorities
- Registry membership is permission to CITE · not authority over NEX truth (per §7.3 audit + R-05.v1.0.0)

**Substrate impact:** seeds `nex.external_authority_registry` (new table · schema authored in this ADR · rows populated at Stage 1 code time) · Guardian rule at citation-record write to validate authority.

**Acceptance criteria:**
- Cited authority resolves to registered value
- Unregistered authority → `unregistered_authority` result · Guardian rejects promotion of AUTHORITATIVE that cites unregistered authority
- Country-scoped filtering works

**Founder decision points:**
- Initial authority list per country
- Reliability scoring methodology
- Attribution template per authority
- Renewal cadence (authorities can lose approved status)
- Licence classification standards

---

### ADR-0314a.2.q · Connection Plausibility Criteria (R-07)

**Purpose:** Define the deterministic criteria for connection plausibility (per R-07 two-tier model: deterministic + subjective candidate flags).

**Dependency:** R-07.v1.0.0 · R-13 relationship vocabulary (ADR-0314a.2.l) · §7.6.5 G4 (Activity as cross-Domain connection).

**Proposed rule (draft):**
- Deterministic tier (Guardian auto-permits): all of:
  1. Declared `relation_kind` resolves to canonical vocabulary (per R-13)
  2. Source endpoint resolvable to canonical entity (via Bridge per ADR-0312)
  3. Target endpoint resolvable to canonical entity
  4. Required evidence/provenance exists per LAM Rule 5
  5. Both endpoints' LAM Object Types permit the declared relation
- Subjective tier (candidate flags for review): fails deterministic but plausible per LLM-suggested connection · never auto-promoted
- Fail-closed default: `plausibility_check_disabled_pending_criteria` · never `false` · never `contradiction`
- `unknown` never silently converts to `contradiction` per §7.7 H1

**Substrate impact:** Guardian rule at relation-record write · writes to `nex.graph_edges` / `nex.relationships` · candidate flags to review queue.

**Acceptance criteria:**
- Deterministic connections auto-permit
- Subjective connections flagged for review (never auto-promote)
- Missing-endpoint case fails deterministic · surfaces to review
- Test N: unavailable evidence → fail-closed UNKNOWN · never contradiction

**Founder decision points:**
- Which LAM Object Type × LAM Object Type pairs permit which relation kinds
- Subjective-tier review cadence (Master AI · founder · both)
- Whether LLM-suggested candidates are captured or discarded

---

### ADR-0314a.2.r · Contradiction Rule Specifications (R-20)

**Purpose:** Define the deterministic rules for cross-record contradiction detection.

**Dependency:** R-20.v1.0.0 · R-13 vocabulary (ADR-0314a.2.l) · R-01 thresholds (ADR-0314a.2.n) · ADR-0314c (Cross-Substrate Contradiction Detection) · §7.7 H1 (`unknown` ≠ `contradiction`).

**Proposed rule (draft):**
- Deterministic contradiction rules:
  1. Same subject (identified via Bridge per ADR-0312) carries logically-incompatible claims (e.g. `is_open_24h = true` AND `closing_hour = 22:00`)
  2. Numeric claims outside R-01 tolerance for a shared attribute (e.g. two sources claim `star_rating: 3` vs `star_rating: 5`)
  3. Categorical claims from different registered authorities (per R-05) disagree
  4. Same-record `record_versions` history shows unauthored regression (a value flips back after founder-declared promotion)
- Explicitly forbidden as contradiction basis:
  - Similarity-based verdicts
  - Keyword-overlap verdicts
  - LLM-alone verdicts (LLM permitted only as candidate-flag producer)
  - Missing-data-as-falsity
  - Scope-mismatch-as-contradiction
- Fail-closed default: `cross_record_detection_pending_rules` when rules not authored

**Substrate impact:** writes to `nex.contradictions` (schema-ready · currently 0 rows) · `nex.fact_conflict` · Guardian rule at write-time to trigger contradiction check.

**Acceptance criteria:**
- Deterministic contradictions detected (fixture cases per ADR-0314a.1)
- Forbidden bases explicitly REJECTED as contradiction triggers
- Missing-data does NOT produce contradiction
- Test G (contradicting sources): Truth Engine records the conflict · does NOT arbitrarily choose
- Test N: unavailable evidence → UNKNOWN · never contradiction

**Founder decision points:**
- Exact rule specifications per attribute type
- Tolerance for numeric-claim contradictions (per R-01 threshold + delta)
- Reconciliation policy when contradictions detected (both sources marked `contradiction_pending` · founder reviews · Guardian rejects further promotion until resolved)
- Similarity-based candidate-flag capture (yes/no · founder decides)

---

### ADR-0317 · NEX Voice Mandate (R-03)

**Purpose:** Define deterministic voice/tone rules for NEX outputs (Chat · NEX1 · composer).

**Dependency:** R-03.v1.0.0 · §7.10 M1 Location metadata (language variants EN-GB · EN-US · ID · etc.).

**Proposed rule (draft):**
- Deterministic tier: rules that can be mechanically checked (banned words · required greeting registers · politeness markers · maximum sentence length · language-consistency requirements per session)
- Subjective tier: candidate flags for review (tone drift · brand voice · founder-specific style)
- Deterministic rules Guardian auto-permits · subjective flagged for review
- Language variants: EN (canonical) · ID (Bahasa Indonesia) · EN-GB (UK expansion Stage 6) · EN-US · EN-AU · en-IE per country activation
- Fail-closed default: `voice_check_disabled_pending_mandate` · subjective tier holds candidate flags

**Substrate impact:** voice-check rule at composer output · rejection of banned-word violations · candidate flags to review queue.

**Acceptance criteria:**
- Deterministic violations rejected at composer output
- Subjective violations flagged for founder review
- Language-variant switching works per session

**Founder decision points:**
- Voice guidelines (formal · informal · warm · concise · founder authors)
- Banned/preferred vocabulary per language variant
- Sentence-length limits
- Country-specific tone conventions (UK vs Indonesia)

---

### ADR-0314c · Cross-Substrate Contradiction Detection

**Purpose:** Detect contradictions across NEX substrates (Supabase legacy knowledge_records · nex_dev specialist tables · repo-hardcoded knowledge) via Bridge-mediated same-subject comparison.

**Dependency:** R-20.v1.0.0 · ADR-0312 Bridge · ADR-0310 Knowledge Router · §7.8 J1 (Physical ≠ logical authority · Router boundary reads physical values · treats them as evidence not authority) · §7.5 D1 (Commerce · Business cross-Domain seller identity) · §7.9 L1 (Services · Business cross-Domain business identity).

**Proposed rule (draft):**
- Cross-substrate comparison via Bridge (ADR-0312) resolves the same real-world entity across Supabase + nex_dev
- Same-subject detection: `nex.mp_seller ↔ nex.business_lead_directory` when same `business_name + city + phone` OR `subject_ref` match
- Reconciliation policy: 
  - Physical Supabase value = evidence (not authoritative) per §7.8 J1
  - Logical Domain value = authoritative per constitutional layer
  - When Supabase legacy value contradicts nex_dev specialist substrate → contradiction recorded · Bridge resolves in favour of specialist substrate (post-migration per ADR-0314a.2.aa)
  - When two specialist substrates disagree → contradiction recorded · founder review
- Idempotent across substrates: same contradiction detected twice → same contradiction row (no duplicates)

**Substrate impact:** read-only across substrates during detection · writes to `nex.contradictions` · Guardian rule at cross-substrate promotion boundary.

**Acceptance criteria:**
- Cross-substrate contradictions detected (fixture cases per ADR-0314a.1)
- Single-source contradictions handled by ADR-0314a.2.r (not this ADR)
- Test G (contradicting sources) passes across substrates
- Test L (idempotency) preserved

**Founder decision points:**
- Reconciliation priority (Supabase vs nex_dev · legacy vs specialist)
- Which substrate wins when conflict is genuine
- Cross-substrate versioning (does a resolved cross-substrate contradiction trigger version rows in both substrates?)
- Bridge relation names for cross-substrate identity assertions

---

### ADR-0314a.1 · Reference Fixture Set

**Purpose:** Author reference fixtures for verifier certification per ADR-0314a rule-parity C-2 criterion. The Fixture Set provides deterministic pass/fail test cases for every R-01–R-20 rule.

**Dependency:** All 10 preceding Stage 1 companion ADRs (must be authored FIRST · fixtures then written to prove them) · ADR-0314a rule-parity certification framework.

**Proposed structure:**
- ~50–100 reference fixture cases · one or more per R-rule
- Fixture format: `{fixture_id, r_rule, input_row, expected_verdict, expected_reason, expected_confidence_band}`
- Cases cover:
  - Positive: valid rows should PASS
  - Negative: invalid rows should FAIL with specific reason
  - Edge: boundary values · unicode · language variants · zero/null/missing
  - Adversarial: malformed · contradictory · corrupt · duplicate (partial coverage · full adversarial in Stage 5)
  - Founder-override: R-10 gate override cases (Stage 2 reference)
- Fixtures live in isolated test schema (`nex_test.fixture_*` or similar) · NEVER touch production nex.* rows
- Fixture set is versioned (per R-17) · immutable at each version · new fixtures added as new versions

**Substrate impact:** isolated test schema · zero production write · fixture rows read-only from production perspective.

**Acceptance criteria:**
- Every R-rule has at least 3 fixtures (positive · negative · edge)
- Verifier passes 100% of fixture set at Stage 1 exit
- Fixture set is deterministic (same input → same verdict every run)
- Fixture set is documented (each fixture has a purpose statement)

**Founder decision points:**
- Fixture count per R-rule
- Adversarial fixture coverage at Stage 1 vs deferred to Stage 5
- Fixture authoring workflow (Master AI drafts · founder approves each · or bulk-approves)
- Fixture retention policy

---

### ADR-0314e · Truth Engine Verifier Implementation (Gate 3 CANDIDATE · doctrine only)

**Purpose:** The verifier design. Consumes R-01–R-20 rules per companion ADRs. Produces `truth_engine_ok` verdicts + R-18 verifier_instance_id + rule_set_version per verdict.

**CORRECTED wording per founder pre-flight directive 2026-09-11 (replaces prior "Gate 3 opens at this ADR's implementation start"):**

> **ADR-0314e is Gate 3 CANDIDATE doctrine. Implementation of the verifier is BLOCKED until the founder explicitly issues "GATE 3 OPEN". Founder-authorisation of this ADR (as doctrine) does NOT open Gate 3. Founder-authorisation of ALL 12 Stage 1 companion ADRs does NOT open Gate 3. Founder-approval of fixture strategy does NOT open Gate 3. Founder-approval of R-10 impact analysis does NOT open Gate 3. Only the founder-issued verbatim statement "GATE 3 OPEN" opens Gate 3. That statement is a separate constitutional write-authorisation event.**

The verifier design (below) is DOCTRINE. Substrate write · Guardian rule installation · verifier code implementation · migration authoring — none of these commence until Gate 3 is explicitly opened.

**Dependency:** All 11 preceding Stage 1 companion ADRs founder-authorised · R-10 gate model doctrine (§0314a.2 consolidation) · ADR-0314a rule-parity certification criteria.

**Proposed architecture (draft):**
- Runtime: TypeScript / Node · consumes Postgres via pg client
- Structure: rule-composable evaluation pipeline · rules fire in parallel (order not doctrinally locked per R-10 gate model 2026-09-11)
- Verdict envelope per row:
  - `verifier_instance_id` (UUID · deterministic per verifier deploy)
  - `rule_set_version` (composed manifest per ADR-0314a rule-parity)
  - `guardian_version`
  - `authorisation_policy_ref` (Stage 2 reference · null at Stage 1)
  - `truth_engine_ok` boolean
  - Per-rule verdict rows in `nex.verifier_verdict` (schema authored here · currently 0 rows)
- Fail-closed defaults per rule (per R-01…R-20 lock text)
- Rule ordering: implementation decides · doctrine locked at gate semantics only (per §7.7 H1 R-10-as-gate)

**Substrate impact (Gate 3 opens for this):**
- Writes to `nex.verifier_verdict` (new table · schema authored) · `nex.confidence_scores` · `nex.record_versions` · `nex.contradictions` · `nex.fact_conflict`
- Reads all existing substrate (accommodation_business · food_business · service_business · knowledge_records · etc.)
- Verifier does NOT promote to AUTHORITATIVE (Stage 2 R-10 gate does that)
- Guardian rules install (extends existing 2,300+ permit / 368+ reject event pipeline)

**Acceptance criteria:**
- Verifier passes ADR-0314a.1 Reference Fixture Set (100%)
- R-10 impact analysis complete (see Part D below)
- Guardian rule installation validated (rejection events logged correctly · anti-substitution enforced)
- Test N (fail-closed): pass across all 12 companion ADR scopes
- Test J-partial: no existing AUTHORITATIVE row silently demoted or corrupted
- **Founder issues explicit "GATE 3 OPEN" authorisation before any of the above executes**

**Founder decision points:**
- Runtime language/platform (TS/Node canonical · founder confirms)
- Deployment mode (in-process · sidecar · scheduled)
- Rollback plan (Guardian rejection log allows revert · founder authors rollback ADR if needed)
- Performance targets (verdict latency · throughput per hour)
- Monitoring / observability (Founder Window integration)
- **GATE 3 OPEN timing:** after this ADR is authorised AND ADR-0314a.1 fixtures are authored AND Part D R-10 impact analysis is founder-reviewed AND founder explicitly issues "GATE 3 OPEN"

---

## Part C · Stage 1 fixture strategy / reference set

Fixture set authored in ADR-0314a.1. This Part C describes the STRATEGY · the actual fixture rows will be authored during Stage 1 code implementation post-Gate-3-open.

**Strategy:**

1. **One-fixture-per-rule minimum:** every R-01…R-20 rule has at least 3 fixtures (positive · negative · edge)
2. **Deterministic reproducibility:** every fixture is `{input, expected_output}` pair · run twice · same output both times
3. **Isolated substrate:** fixtures live in `nex_test.*` schema (or similar) · never touch production nex.* rows
4. **Cross-referenced against feedback memories:** fixtures include cases that verify the 6 constitutional discipline rules:
   - Operational scope vs subject-matter Domain (per feedback_domain_operational_vs_subject_matter.md)
   - Architectural not linguistic reasoning (per feedback_decide_taxonomy_on_architecture_not_linguistics.md)
   - No Domain inflation from orphan slugs (per feedback_no_domain_inflation_from_orphan_slugs.md)
   - Activity ≠ Domain (per feedback_accommodation_does_not_own_rental.md)
   - other ≠ unknown (per feedback_other_vs_unknown_never_silently_converted.md)
   - Constitutional-first sequencing (per feedback_finish_constitutional_layer_before_100k_questions.md)
5. **Versioned:** fixture set is versioned per R-17 · immutable at each version · new fixtures added as new versions
6. **Fixture authoring workflow:** Master AI drafts fixture cases · founder reviews · founder approves each OR bulk-approves per rule
7. **Adversarial fixtures at Stage 5:** Stage 1 fixtures are REFERENCE cases (positive · negative · edge). Adversarial fixtures (malformed · contradictory · corrupt · duplicate · idempotent challenges) are authored at Stage 5 per ADR-0314a.2.ac
8. **Cross-rule fixtures:** some fixtures test multiple R-rules simultaneously (e.g. a row that requires R-01 plausibility AND R-11 confidence AND R-18 verifier_instance_id · all three should pass or fail together)

**Fixture set version 1 target:** ~50–75 fixture cases at Stage 1 exit · covering R-01/R-03(det)/R-05/R-07(det)/R-11/R-12/R-13/R-17/R-18/R-20(det). R-10 (Stage 2) and R-02 (Stage 3) fixtures added at their respective stages.

---

## Part D · R-10 Impact Analysis (live substrate probe · 2026-09-11)

**Founder-required deliverable · per Stage 1 acceptance criteria.**

**Live substrate scope (read-only probe 2026-09-11):**

| Status | authorised_by | Row count |
|---|---|---:|
| AUTHORITATIVE | null | **175** (revised from earlier estimate of 93) |
| AUTHORITATIVE | non-null | 22 |
| AUTHORITATIVE | total | 197 |

**Impact set: 175 AUTHORITATIVE rows in Supabase `knowledge_records` currently carrying `authorised_by = null`.**

Per R-10.v1.0.0 doctrine: *"Verifier verdicts terminate at `truth_engine_ok` unless the target LAM object type's authorisation policy explicitly permits autonomous promotion to `AUTHORITATIVE`."*

These 175 rows carry AUTHORITATIVE status without an authorising identity. Under Stage 2 per-LAM-object-type authorisation policies (ADR-0314a.2.a-h), each of the 175 must be reconciled:

- OPTION 1: retroactive authorised_by field populated with `founder_legacy_pre_R10` (or similar sentinel · founder authors)
- OPTION 2: rows demoted to `truth_engine_ok` (pending Stage 1 verifier verdict + Stage 2 R-10 gate approval)
- OPTION 3: per-row founder review (impractical at 175 rows)
- OPTION 4: hybrid — subject-matter tier (Trade Knowledge · Components · Design & Engineering) bulk-tagged with `founder_legacy` sentinel; operational-scope tier (AI Rules · Business Operating System · Platform Architecture) reviewed individually

**Category distribution of the 175 rows (top-20):**

| Rows | Category |
|---:|---|
| 23 | `NEX Components` |
| 18 | `NEX Trade Knowledge · Components · Staircase Components` |
| 13 | `NEX Trade Knowledge` |
| 13 | `NEX Technical Guidance` |
| 11 | `NEX Customer Guidance` |
| 6 | `NEX Platform Architecture · Knowledge Management` |
| 5 | `NEX Trade Knowledge · Design & Engineering · Geometries` |
| 4 | `NEX Trade Knowledge · Design & Engineering` |
| 3 | `NEX Trade Knowledge · Historical Context` |
| 3 | `NEX Trade Knowledge · Design · Ergonomics & Spatial Design` |
| 3 | `NEX Components · Staircase · Structural Joinery` |
| 2 | `NEX Trade Knowledge · Processes · Manufacturing` |
| 2 | `NEX Trade Knowledge · Processes · Installation` |
| 2 | `NEX Customer Guidance · Decision Support` |
| 2 | `NEX Trade Knowledge · Design & Engineering · Structural Mechanics` |
| 2 | `NEX Trade Knowledge · Structural Framing · Stairwell Openings` |
| 2 | `NEX Components · Staircase · Balustrade bottom rail` |
| 2 | `NEX Components · Handrail Specifications` |
| ... (long tail of 40+ single-row categories) | |

**Interpretation:**

- **~85% of the 175 rows are Trade Knowledge subject-matter** (Trade Knowledge · Components · Design & Engineering · Historical Context · etc.) — these predate the R-10 gate and are legitimate content · just lacking the R-10 audit trail
- **~15% are operational-scope tier** (Platform Architecture · Business Operating System · AI Rules · Technical Guidance) — these may need different reconciliation policy
- **Zero rows are Language / Semantic / People-Say / Confidence / Version / etc. LAM Object Types** — the 175 all fall under Domain Knowledge LAM Object Type (candidate future Trade Knowledge Domain per §7.10 M1 · currently living in Supabase legacy substrate)

**Recommended reconciliation (draft · founder decides at Stage 2):**

- 175 rows carry a synthetic `authorised_by = "founder_legacy_pre_R10_v1"` sentinel to preserve AUTHORITATIVE status
- Sentinel is founder-authored per Stage 2 ADR-0314a.2.i (R-10 Impact Analysis ADR)
- No content change to the 175 rows
- Guardian permits the sentinel value at authorised_by field
- Future R-10 gate policies (ADR-0314a.2.a-h) treat the sentinel as founder-legacy-authorised
- Optional: subject the 175 rows to re-verification per Stage 1 verifier · verdicts recorded but sentinel authorisation preserved

**What this Part D did NOT do:**
- No row modified · 175 rows preserved unchanged
- No sentinel value applied · authoring deferred to Stage 2 ADR-0314a.2.i
- No verifier verdict produced (Gate 3 CLOSED)
- No LAM Object Type reclassification (Stage 2 authoring)

---

## Part E · What this Stage 1 batch document did NOT do

- ❌ Gate 3 remains CLOSED
- ❌ No verifier code written
- ❌ No Guardian rule installed (installation at Stage 1 code time post-Gate-3-open)
- ❌ No migration authored
- ❌ No substrate row modified · 175 R-10-impact rows preserved · 3,627 Supabase rows preserved · 22 authorised_by=nonnull rows preserved · all nex_dev rows preserved
- ❌ No CHECK constraint installed
- ❌ No fixture row populated (fixtures live in isolated test schema · authored at Stage 1 code time)
- ❌ No R-10 gate wired (Stage 2)
- ❌ No Category taxonomy populated (Stage 3)
- ❌ No autonomous loop wired (Stage 4)
- ❌ No adversarial test executed (Stage 5)
- ❌ No international expansion (Stage 6)
- ❌ No 100K corpus authored (Stage 6 · blocked until Stage 4/5)
- ❌ No feedback memory modified
- ❌ No constitutional rule modified · frozen

---

## Part F · Explicit founder approval request

Master AI STOPS. Founder is requested to:

1. **Review each of the 12 companion ADRs** in Part B above · amend the draft rules · decide the founder decision points · authorise or amend each ADR individually or in batch
2. **Review the Stage 1 Fixture Strategy** in Part C · approve or amend
3. **Review the R-10 Impact Analysis** in Part D · decide reconciliation approach (Option 1/2/3/4 or amend) · this becomes Stage 2 ADR-0314a.2.i authoring input
4. **Explicitly authorise proceeding to substrate write** — Gate 3 CLOSED remains until founder issues "GATE 3 OPEN"

**Substrate write authorisation event (per founder verbatim 2026-09-11):**

> *"the founder must separately issue GATE 3 OPEN. That is the actual constitutional write-authorisation event."*

**Sequence of Gate 3 opening:**

Stage 1 ADRs authored → founder reviews each → founder authorises each → fixture strategy approved → R-10 impact analysis approved → founder authors per-ADR final wording → founder explicitly says **"GATE 3 OPEN"** → verifier code implementation begins → Guardian rules install → substrate writes begin.

**Master AI does NOT open Gate 3 autonomously. Master AI does NOT begin verifier code implementation. Master AI does NOT modify any substrate. Master AI does NOT author migrations.**

Gate 3 CLOSED. Freeze holds. Zero substrate touched.

---

**End of Stage 1 Companion ADR Batch document.**

**Master AI STOPS here.** Awaiting explicit founder review of the 12 ADRs · fixture strategy · R-10 impact analysis · and separate "GATE 3 OPEN" authorisation. Gate 3 remains CLOSED regardless of ADR authorisation status.
