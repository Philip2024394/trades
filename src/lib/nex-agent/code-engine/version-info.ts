// src/lib/nex-agent/code-engine/version-info.ts
//
// Publishes NEX1 Code Authoring Engine sprint metadata · authored by NEX1 via the template-only identity-floor adapter.
//
// Authored by NEX1 via the template-only identity-floor adapter.

/** Current NEX1 Code Authoring Engine sprint number. */
export const NEX1_CODE_ENGINE_SPRINT = 1 as const;

/** Identity-floor adapter id · NEX1's non-negotiable authoring capability. */
export const NEX1_CODE_ENGINE_IDENTITY_FLOOR = "template-only" as const;

/** The accountable programming agent · never the adapter · never a vendor. */
export const NEX1_CODE_ENGINE_OWNER = "NEX1" as const;

/** Constitutional statement · Amendment 1 · 2026-09-12. */
export const NEX1_CODE_ENGINE_CONSTITUTION = "NEX1 owns the programming loop. "
  + "Reasoning adapters assist within the code-proposal sub-step only. "
  + "No third-party runtime dependency. "
  + "Model is a tool NEX1 may use · not NEX1.";
