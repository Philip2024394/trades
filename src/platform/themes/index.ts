// themeRegistry — barrel. Side-effect registers the 6 preset seeds.

import "./presetSeeds";

export { REGISTRY_METADATA, themeRegistry } from "./registry";
export type {
  FrozenThemeManifest,
  ThemeManifest,
  ThemeMotion,
  ThemePresetId,
  ThemeVars
} from "./types";
