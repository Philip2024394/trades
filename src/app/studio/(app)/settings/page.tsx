import { StudioStubPage } from "@/components/studio/StudioStubPage";

export const dynamic = "force-dynamic";

export default function StudioSettingsPage() {
  return (
    <StudioStubPage
      title="Settings"
      subtitle="Merchant profile, contact details, notifications, integrations and security. Everything that isn't editing lives here."
      moduleNumber="Post-0"
      moduleName="Rolled into shell polish"
    />
  );
}
