// Contacts — personal relationship hub inside NEX. Dark premium
// theme, distinct from the cream Discover / Platform surfaces so it
// feels like the user's own private space for people + groups.

import { ContactsShell } from "@/components/nex-app/contacts/ContactsShell";

export const dynamic = "force-dynamic";

export default function ContactsPage() {
  return <ContactsShell />;
}
