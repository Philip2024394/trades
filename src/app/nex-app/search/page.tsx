// src/app/nex-app/search/page.tsx
// Founder Phase 29 · unify NEX under /nex-app · re-export of the canonical
// /nex/search page so both URLs render the same component (single source
// of truth). Client-side page · "use client" directive on the source file
// applies here too.

export { default } from "@/app/nex/search/page";
