// NEX Design Language · Phase 1 showcase
// 25 distinct card styles in the NEX off-white premium theme.
// Any NEX surface can compose from these reusable blocks — the same
// design language will power Trade Centre, Discover, Contacts,
// Business, Marketplace and everything that comes next.

import { NexCardGallery } from "@/components/nex-app/design-language/NexCardLibrary";

export const dynamic = "force-dynamic";

export default function DesignSystemPage() {
  return <NexCardGallery />;
}
